name: 01 - Building Blocks

on: push

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Say hello
              run: echo "Hello, World!"
    echo-goodbuy:
        runs-on: ubuntu-latest
        steps:
            - name: Success Step
              run: |
                    echo "I will not fail!"
                    exit 0
            - name: Good Bye
              run: echo "Goodbye!"
